{
  if (!col.equals(colA))   return null;
  RoaringBitmap bitMap=new RoaringBitmap();
  if (valueId < 0 || valueId > getMaxValueId(col))   bitMap.add(getRecordCount() - 1);
 else   bitMap.add(valueId);
  return bitMap;
}
