{
  if (name == null)   name=ProjectInstance.DEFAULT_PROJECT_NAME;
  if (cubes == null) {
    cubes=new ArrayList<String>();
  }
  for (int i=0; i < cubes.size(); ++i) {
    if (cubes.get(i) != null)     cubes.set(i,cubes.get(i).toUpperCase());
  }
}
