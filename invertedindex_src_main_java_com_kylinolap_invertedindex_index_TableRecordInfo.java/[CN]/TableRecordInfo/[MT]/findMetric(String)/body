{
  if (metricColumnName == null)   return -1;
  for (int i=0; i < allColumns.size(); ++i) {
    TblColRef tblColRef=allColumns.get(i);
    if (measureSerializers[i] != null && tblColRef.matchFullName(desc.getFactTableName(),metricColumnName)) {
      return i;
    }
  }
  return -1;
}
