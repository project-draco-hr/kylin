{
  ExecutableDao r=CACHE.get(config);
  if (r == null) {
synchronized (ExecutableDao.class) {
      r=CACHE.get(config);
      if (r == null) {
        r=new ExecutableDao(config);
        CACHE.put(config,r);
      }
    }
  }
  return r;
}
