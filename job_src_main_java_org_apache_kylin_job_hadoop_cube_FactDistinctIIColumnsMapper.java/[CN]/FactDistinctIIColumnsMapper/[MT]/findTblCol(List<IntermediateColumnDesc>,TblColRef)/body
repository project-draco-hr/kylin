{
  return Iterators.indexOf(columns.iterator(),new Predicate<IntermediateColumnDesc>(){
    @Override public boolean apply(    IntermediateColumnDesc input){
      return input.getColRef().equals(col);
    }
  }
);
}
