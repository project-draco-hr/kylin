{
  List<ColumnMetaData> meta=new ArrayList<ColumnMetaData>();
  for (int i=0; i < 10; i++) {
    meta.add(ColumnMetaData.dummy(ColumnMetaData.scalar(Types.VARCHAR,"varchar",Rep.STRING),true));
  }
  List<MetaTable> tables=new ArrayList<MetaTable>();
  MetaTable table=new MetaTable("dummy","dummy","dummy","dummy","dummy","dummy","dummy","dummy","dummy","dummy");
  tables.add(table);
  return new MetaProject(null,null,new DataSet<MetaTable>(meta,new KylinEnumerator<MetaTable>(tables)),null);
}
