{
  boolean isEmptyAnd=false;
switch (filter.getOperator()) {
case EQ:
case IN:
case LT:
case LTE:
case GT:
case GTE:
    if (values == null || values.isEmpty()) {
      isEmptyAnd=true;
    }
  break;
default :
break;
}
return isEmptyAnd;
}
