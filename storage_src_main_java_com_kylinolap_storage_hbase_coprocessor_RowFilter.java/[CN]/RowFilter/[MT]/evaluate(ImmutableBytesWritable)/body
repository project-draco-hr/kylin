{
  if (flatOrAndFilter == null || flatOrAndFilter.length == 0)   return true;
  boolean r=false;
  for (int i=0, n=flatOrAndFilter.length; i < n && r == false; i++) {
    r=evaluateAndFilter(rowkey,flatOrAndFilter[i]);
  }
  return r;
}
