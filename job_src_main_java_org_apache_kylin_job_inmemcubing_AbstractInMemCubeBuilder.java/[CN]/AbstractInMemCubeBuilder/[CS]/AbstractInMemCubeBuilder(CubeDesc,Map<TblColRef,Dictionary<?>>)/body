{
  if (cubeDesc == null)   throw new NullPointerException();
  if (dictionaryMap == null || dictionaryMap.isEmpty())   throw new IllegalArgumentException("dictionary cannot be empty");
  this.cubeDesc=cubeDesc;
  this.dictionaryMap=dictionaryMap;
}
