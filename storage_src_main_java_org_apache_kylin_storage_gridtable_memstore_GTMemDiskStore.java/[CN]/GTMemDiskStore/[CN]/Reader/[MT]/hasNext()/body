{
  if (next != null)   return true;
  try {
    if (din.available() > 0) {
      block.importFrom(din);
      next=block;
    }
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  return next != null;
}
