{
  int sum=0;
  for (int i=0; i < groups.length; i++) {
    String[] group=groups[i];
    int result=validateGroup1(set,context,group);
    sum=sum + result;
  }
  if (sum > 1) {
    context.addResult(ResultLevel.ERROR,"Hierachy column [" + set + "] was referenced in "+ sum+ " groups ");
  }
}
