{
  List<IISegment> readySegs=getSegments(SegmentStatusEnum.READY);
  long startTime=Long.MAX_VALUE;
  for (  IISegment seg : readySegs) {
    if (seg.getDateRangeStart() < startTime)     startTime=seg.getDateRangeStart();
  }
  return startTime;
}
