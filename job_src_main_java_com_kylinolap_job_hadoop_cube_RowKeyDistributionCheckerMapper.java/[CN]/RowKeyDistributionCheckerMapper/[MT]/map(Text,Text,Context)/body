{
  for (  Text t : keyList) {
    if (key.compareTo(t) < 0) {
      Long v=resultMap.get(t);
      long length=key.getLength() + value.getLength();
      v+=length;
      resultMap.put(t,v);
      break;
    }
  }
}
