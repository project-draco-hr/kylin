{
  if (codeSystem == null)   throw new IllegalStateException();
  if (primaryKey == null && primaryKey.cardinality() == 0)   throw new IllegalStateException();
  codeSystem.init(this);
  validateColumnBlocks();
}
