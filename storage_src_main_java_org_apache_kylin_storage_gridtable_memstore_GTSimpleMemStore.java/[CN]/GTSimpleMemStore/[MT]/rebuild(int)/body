{
  rowBlockList.clear();
  return new IGTStoreWriter(){
    @Override public void close() throws IOException {
    }
    @Override public void write(    GTRowBlock block) throws IOException {
      rowBlockList.add(block.copy());
    }
  }
;
}
