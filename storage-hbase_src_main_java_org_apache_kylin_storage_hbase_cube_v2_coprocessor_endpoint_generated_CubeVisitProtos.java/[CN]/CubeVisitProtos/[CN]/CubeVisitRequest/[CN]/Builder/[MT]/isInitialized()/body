{
  if (!hasBehavior()) {
    return false;
  }
  if (!hasGtScanRequest()) {
    return false;
  }
  if (!hasHbaseRawScan()) {
    return false;
  }
  if (!hasRowkeyPreambleSize()) {
    return false;
  }
  if (!hasStartTime()) {
    return false;
  }
  if (!hasTimeout()) {
    return false;
  }
  if (!hasKylinProperties()) {
    return false;
  }
  return true;
}
