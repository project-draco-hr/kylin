{
  if (!hasGtScanRequest()) {
    return false;
  }
  if (!hasHbaseRawScan()) {
    return false;
  }
  if (!hasRowkeyPreambleSize()) {
    return false;
  }
  if (!hasKylinProperties()) {
    return false;
  }
  return true;
}
