{
  Set<String> temp=new TreeSet<>(String.CASE_INSENSITIVE_ORDER);
  if (stringSets != null) {
    for (    String[] ss : stringSets) {
      temp.clear();
      for (      String s : ss) {
        temp.add(s);
        dims.add(s);
      }
      dimsList.add(temp);
    }
  }
}
