{
  final SortedMap<byte[],Object> map=new TreeMap<byte[],Object>(new Comparator<byte[]>(){
    @Override public int compare(    byte[] o1,    byte[] o2){
      return Bytes.compareTo(o1,o2);
    }
  }
);
  final Random rand=new Random();
  int est=estimateObjectSize(new CreateAnObject(){
    @Override public Object create(){
      byte[] key=new byte[10];
      rand.nextBytes(key);
      map.put(key,null);
      return null;
    }
  }
);
  System.out.println("TreeMap entry: " + (est - 20));
}
