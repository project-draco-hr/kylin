{
  StringBuffer sb=new StringBuffer();
  sb.append("[");
  for (  IRealization r : realizations) {
    sb.append(r.getName());
    sb.append(",");
  }
  sb.deleteCharAt(sb.length() - 1);
  sb.append("]");
  return sb.toString();
}
