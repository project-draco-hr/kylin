{
  double r=BASE_CUBOID_CACHE_OVERSIZE_FACTOR;
  for (  MeasureDesc m : cubeDesc.getMeasures()) {
    if (m.getFunction().getMeasureType().isMemoryHungry())     r+=BASE_CUBOID_CACHE_OVERSIZE_FACTOR;
  }
  return r;
}
