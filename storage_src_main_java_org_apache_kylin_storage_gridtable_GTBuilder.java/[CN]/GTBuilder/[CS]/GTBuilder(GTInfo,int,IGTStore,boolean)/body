{
  this.info=info;
  block=GTRowBlock.allocate(info);
  blockWriter=block.getWriter();
  if (append) {
    storeWriter=store.append(shard,blockWriter);
    if (block.isFull())     blockWriter.clearForNext();
  }
 else {
    storeWriter=store.rebuild(shard);
  }
}
