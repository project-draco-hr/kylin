{
  JobDAO r=CACHE.get(config);
  if (r == null) {
    r=new JobDAO(config);
    CACHE.put(config,r);
    if (CACHE.size() > 1) {
      logger.warn("More than one singleton exist");
    }
  }
  return r;
}
