{
  StringBuilder buf=new StringBuilder();
  buf.append("[");
  Object[] values=getValues();
  for (int i=0; i < values.length; i++) {
    if (i > 0)     buf.append(", ");
    buf.append(values[i]);
  }
  buf.append("]");
  return buf.toString();
}
