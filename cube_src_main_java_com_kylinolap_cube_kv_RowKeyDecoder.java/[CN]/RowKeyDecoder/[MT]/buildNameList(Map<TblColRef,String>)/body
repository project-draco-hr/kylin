{
  List<TblColRef> columnList=getColumns();
  List<String> result=new ArrayList<String>(columnList.size());
  for (  TblColRef col : columnList)   result.add(findName(col,aliasMap));
  return result;
}
