{
  if (singleBucket == -1) {
    return 0;
  }
 else   if (singleBucket >= 0) {
    return 1;
  }
 else {
    int size=0;
    for (int i=0; i < m; i++) {
      if (registers[i] > 0)       size++;
    }
    return size;
  }
}
