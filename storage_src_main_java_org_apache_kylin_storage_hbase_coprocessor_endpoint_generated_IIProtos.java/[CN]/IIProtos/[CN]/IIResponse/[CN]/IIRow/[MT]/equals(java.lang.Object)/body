{
  if (obj == this) {
    return true;
  }
  if (!(obj instanceof IIProtos.IIResponse.IIRow)) {
    return super.equals(obj);
  }
  IIProtos.IIResponse.IIRow other=(IIProtos.IIResponse.IIRow)obj;
  boolean result=true;
  result=result && (hasColumns() == other.hasColumns());
  if (hasColumns()) {
    result=result && getColumns().equals(other.getColumns());
  }
  result=result && (hasMeasures() == other.hasMeasures());
  if (hasMeasures()) {
    result=result && getMeasures().equals(other.getMeasures());
  }
  result=result && getUnknownFields().equals(other.getUnknownFields());
  return result;
}
