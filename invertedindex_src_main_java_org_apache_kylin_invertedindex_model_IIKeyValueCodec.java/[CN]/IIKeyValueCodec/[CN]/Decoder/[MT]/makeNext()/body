{
  if (containers != null) {
    slice=new Slice(info,lastShard,lastSliceTimestamp,containers);
    slice.setLocalDictionaries(Maps.newHashMap(localDictionaries));
  }
  lastSliceTimestamp=Long.MIN_VALUE;
  lastCol=-1;
  containers=null;
  localDictionaries.clear();
}
