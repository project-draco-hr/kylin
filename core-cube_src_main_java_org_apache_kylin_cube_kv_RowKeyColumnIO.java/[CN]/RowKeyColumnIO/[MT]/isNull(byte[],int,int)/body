{
  if (length == 0) {
    return false;
  }
  for (int i=0; i < bytes.length; i++) {
    if (bytes[i + offset] != AbstractRowKeyEncoder.DEFAULT_BLANK_BYTE) {
      return false;
    }
  }
  return true;
}
