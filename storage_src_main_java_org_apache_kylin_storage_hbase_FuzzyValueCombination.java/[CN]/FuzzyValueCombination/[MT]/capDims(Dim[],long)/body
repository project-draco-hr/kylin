{
  Arrays.sort(dims,new Comparator<Dim>(){
    @Override public int compare(    Dim o1,    Dim o2){
      return -(o1.values.size() - o2.values.size());
    }
  }
);
  for (  Dim dim : dims) {
    if (combCount(dims) < cap)     break;
    dim.values=Collections.emptySet();
  }
}
