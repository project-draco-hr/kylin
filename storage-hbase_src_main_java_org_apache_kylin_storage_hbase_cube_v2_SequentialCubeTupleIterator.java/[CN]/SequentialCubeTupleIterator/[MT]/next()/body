{
  if (next == null) {
    hasNext();
    if (next == null)     throw new NoSuchElementException();
  }
  scanCount++;
  if (++scanCountDelta >= 1000)   flushScanCountDelta();
  ITuple result=next;
  next=null;
  return result;
}
