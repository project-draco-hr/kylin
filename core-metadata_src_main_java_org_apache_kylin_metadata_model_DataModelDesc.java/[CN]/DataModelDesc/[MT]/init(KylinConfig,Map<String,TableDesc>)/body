{
  this.config=config;
  lookupTableRefs.clear();
  aliasMap.clear();
  tableNameMap.clear();
  initTableAlias(tables);
  initJoinColumns();
  ModelDimensionDesc.capicalizeStrings(dimensions);
  initPartitionDesc();
}
