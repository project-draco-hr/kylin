{
  IIProtos.IIRequest.Builder builder=IIProtos.IIRequest.newBuilder();
  if (this.tsInterval != null) {
    if (this.tsInterval.getLeft() != null) {
      builder.setTsStart(this.tsInterval.getLeft());
    }
    if (this.tsInterval.getRight() != null) {
      builder.setTsEnd(this.tsInterval.getRight());
    }
  }
  builder.setType(ByteString.copyFrom(CoprocessorRowType.serialize(pushedDownRowType))).setFilter(ByteString.copyFrom(CoprocessorFilter.serialize(pushedDownFilter))).setProjector(ByteString.copyFrom(CoprocessorProjector.serialize(pushedDownProjector))).setAggregator(ByteString.copyFrom(EndpointAggregators.serialize(pushedDownAggregators)));
  IIProtos.IIRequest request=builder.build();
  return request;
}
